/**
 * @fileoverview PostCSS configuration for Plant Sharing Community.
 * Processes Tailwind CSS directives and adds vendor prefixes for browser compatibility.
 * 
 * Plugins:
 * - tailwindcss: Process Tailwind utility classes
 * - autoprefixer: Add vendor prefixes for cross-browser support
 */
module.exports = {
    plugins: {
        tailwindcss: {},
        autoprefixer: {},
    },
}